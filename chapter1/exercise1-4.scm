; The procedure results in the sum of 'a' and the absolute value of 'b', this
; is accomplished by the conditional in the 'if' expression which perform addition
; if b is a positive number, and subtraction if b is a negative number.

; The intuition behind this is that a + b is the same as a - (-b).
